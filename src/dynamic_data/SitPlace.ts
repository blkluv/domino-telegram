export enum SitPlace {
    TOP = "top",
    LEFT = "left",
    BOTTOM = "bottom",
    RIGHT = "right",
    NONE = "none"
}

export type SitPlaceDefined = SitPlace.TOP | SitPlace.BOTTOM | SitPlace.LEFT | SitPlace.RIGHT;